import {StyleSheet} from 'react-native';

const style = StyleSheet.create({
  inputSearch: {
    borderColor: '#888',
    borderWidth: 1,
    marginTop: '10%',
    margin: 10,
    color: '#000000',
    overflow: 'scroll',
  },
  textInput: {
    height: 38,

    fontSize: 16,
  },
});
export default style;
